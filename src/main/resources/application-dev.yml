spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crm
    username: postgres
    password: 6881

  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
      path: /h2-console